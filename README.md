# txline-rs

Empty Rust SDK scaffold for TxLINE.

This workspace is intentionally structure-only. It defines module boundaries for
future TxLINE network configuration, authentication, subscriptions, HTTP data
access, SSE streams, Solana helpers, and validation flows.

No business logic, HTTP calls, Solana transactions, signing, SSE parsing, or
proof validation is implemented yet.
