//! Activation integration test scaffold.
//!
//! Future tests should cover activation preimage construction, especially
//! `${txSig}:${selectedLeagues.join(",")}:${jwt}` and `${txSig}::${jwt}` for an
//! empty league list. Do not put real JWTs or API tokens in fixtures.
