//! Fixture snapshot, update, and validation endpoint scaffolding.
//!
//! Planned coverage includes `/api/fixtures/snapshot`,
//! `/api/fixtures/updates/{epochDay}/{hourOfDay}`, fixture validation, and
//! batch validation. Implementations should use both TxLINE credentials and
//! preserve network consistency with the selected config.
