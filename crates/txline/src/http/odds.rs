//! Odds snapshot, update, stream, and validation endpoint scaffolding.
//!
//! Planned coverage includes `/api/odds/snapshot/{fixtureId}`,
//! `/api/odds/updates/...`, `/api/odds/stream`, and `/api/odds/validation`.
//! Market availability should be read from responses for each fixture rather
//! than assumed from competition coverage alone.
